# 示例配置,真实文件请复制为 deepseek.yml 并填入 api_key
# 真实 deepseek.yml 已加入 .gitignore,不会被提交
api_key: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
model: deepseek-chat
endpoint: https://api.deepseek.com/chat/completions
temperature: 0.3
timeout_seconds: 60
